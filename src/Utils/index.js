import { createContext } from "react";

const ThemeContext = createContext(null);
const ThemeContext1 = createContext(null);
const ThemeContext2 = createContext(null);
const ThemeContext3 = createContext(null);

export { ThemeContext };
